
// Create the frontend routes:

// In this file app.js, create the necessary routes to handle the CRUD operations using JavaScript and the Fetch API.
// Use the following endpoints for the frontend routes:
// POST /employees: To add a new employee.
// GET /employees: To retrieve all existing employees.
// GET /employees/:id: To retrieve data about a specific employee.
// DELETE /employees/:id: To delete an employee.
// PUT /employees/:id: To edit an employee.
// Use the Fetch API to make HTTP requests to these endpoints and update the UI accordingly.